# Сводка исправлений для Production деплоя

## 🔴 Критические проблемы (исправлены)

### 1. Nginx proxy_pass неправильно настроен
**Файл:** `deploy.sh`  
**Проблема:** `proxy_pass http://localhost:$API_PORT/;` убирал `/api` из пути  
**Исправление:** Изменено на `proxy_pass http://localhost:$API_PORT/api;`  
**Результат:** Запросы к `/api/*` теперь правильно проксируются на API

### 2. CORS не разрешал production домен
**Файл:** `api/server.js`  
**Проблема:** CORS был настроен только на `localhost:3000`  
**Исправление:** Добавлена поддержка множественных origins и автоматическое разрешение для development  
**Результат:** Запросы с `localhost:8080` теперь разрешены

### 3. Отсутствие важных proxy заголовков
**Файл:** `deploy.sh`  
**Проблема:** Nginx не передавал информацию о клиенте  
**Исправление:** Добавлены заголовки `X-Real-IP`, `X-Forwarded-For`, `X-Forwarded-Proto`  
**Результат:** API получает правильную информацию о клиенте

## ⚠️ Требуют ручной настройки

### 1. .env файлы
**Что нужно сделать:**
- В `api/.env` установите:
  ```env
  DISCORD_REDIRECT_URI=http://localhost:8080/login
  CORS_ORIGIN=http://localhost:8080,http://localhost:3000
  ```
- В `dashboard/.env` убедитесь, что есть:
  ```env
  VITE_DISCORD_CLIENT_ID=your_client_id
  VITE_API_URL=/api
  ```

### 2. Discord Application
**Что нужно сделать:**
- Откройте https://discord.com/developers/applications
- Выберите ваше приложение
- В разделе OAuth2 → Redirect URIs добавьте: `http://localhost:8080/login`

## 📋 Изменённые файлы

1. ✅ `deploy.sh` - исправлен proxy_pass и добавлены заголовки
2. ✅ `api/server.js` - улучшена CORS конфигурация
3. ✅ `dashboard/src/App.jsx` - убран лишний basename (не нужен с base: './')
4. ✅ Создан `check-config.sh` - скрипт для проверки конфигурации
5. ✅ Создан `PRODUCTION_FIXES.md` - подробная документация

## 🚀 Быстрый старт после исправлений

1. **Обновите .env файлы** (см. выше)

2. **Проверьте конфигурацию:**
   ```bash
   ./check-config.sh
   ```

3. **Запустите деплой:**
   ```bash
   ./deploy.sh
   ```

4. **Проверьте работу:**
   - Откройте http://localhost:8080
   - Проверьте консоль браузера (F12) - не должно быть 404
   - Попробуйте авторизацию через Discord

## ✅ Ожидаемый результат

После всех исправлений:
- ✅ Фронтенд загружается на http://localhost:8080
- ✅ Нет 404 ошибок в консоли браузера
- ✅ API запросы работают через `/api/*`
- ✅ Discord OAuth авторизация работает
- ✅ После авторизации пользователь попадает на `/dashboard`

## 🐛 Если проблемы остались

1. **Проверьте логи Nginx:**
   ```bash
   tail -f /opt/homebrew/var/log/nginx/error.log
   ```

2. **Проверьте логи API:**
   ```bash
   pm2 logs amaribot-api
   ```

3. **Проверьте конфигурацию:**
   ```bash
   ./check-config.sh
   ```

4. **Смотрите подробную документацию:**
   - `PRODUCTION_FIXES.md` - детальные инструкции
   - `DEPLOYMENT.md` - общая документация по деплою

