sudo npm install -g pm2
#!/bin/bash

# Скрипт для сборки фронтенда
# Использование: ./build.sh

set -e

echo "🔨 Сборка фронтенда Dashboard..."

cd dashboard

# Проверка .env
if [ ! -f ".env" ]; then
    echo "⚠️  Файл .env не найден, используем значения по умолчанию"
fi

# Установка зависимостей (если нужно)
if [ ! -d "node_modules" ]; then
    echo "📦 Установка зависимостей..."
    npm install
fi

# Сборка
echo "🔨 Запуск сборки..."
npm run build

echo "✅ Сборка завершена! Файлы находятся в dashboard/dist/"



